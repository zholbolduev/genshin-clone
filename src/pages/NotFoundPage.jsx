import React from "react";
import NotFound from "../components/NotFound/NotFound";

const NotFoundPage = () => {
  return (
    <div>
      NotFoundPage
      <NotFound />
    </div>
  );
};

export default NotFoundPage;
